<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Rucksack'd</title>
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" />
    <link rel="stylesheet" href="/styles/header.css">
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- Google Fonts: Poppins -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet" />
    <!-- Tailwind CSS -->
   

</head>
<body class="font-poppins">
    <nav class="material-header w-full flex justify-between items-center px-6 py-4">
        <h3 class="text-2xl font-semibold text-gray-800">Rucksack'd</h3>
        <% var loggedin = typeof loggedin !== 'undefined' ? loggedin:true %>
        <% if(loggedin){ %> 
            <div class="flex gap-6 text-gray-700">
                <a href="/shop" class="hover:text-gray-900 transition-colors duration-300">Shop</a>
                <a href="/cart" class="hover:text-gray-900 transition-colors duration-300">Cart</a>
                <a href="" class="hover:text-gray-900 transition-colors duration-300">My Account</a>
                <a href="/users/logout" class="text-red-500 hover:text-red-700 transition-colors duration-300">Logout</a>
            </div>
        <% } %>
    </nav>
</body>
</html>
